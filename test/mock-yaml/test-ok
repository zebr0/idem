---
- echo "1"
- echo "1" > tmp/retry
- retry=`cat tmp/retry` && echo $(($retry+1)) > tmp/retry && [ $retry -eq 3 ]
# comment
- include: include
- lookup: lookup
  path: tmp/lookup
- script: old syntax
- - true
