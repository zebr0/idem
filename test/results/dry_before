  todo echo "1"
  todo echo "1" > tmp/retry
  todo retry=`cat tmp/retry` && echo $(($retry+1)) > tmp/retry && [ $retry -eq 3 ]
  todo echo "2"
  todo echo "3"
  todo {'lookup': 'lookup', 'path': 'tmp/dir/lookup'}
unknown command, ignored: {'script': 'old syntax'}
unknown command, ignored: ['true']
