executing echo "1"
1
done
executing echo "1" > tmp/retry
done
executing retry=`cat tmp/retry` && echo $(($retry+1)) > tmp/retry && [ $retry -eq 3 ]
retrying
retrying
done
executing echo "2"
2
done
executing echo "3"
3
done
executing {'lookup': 'lookup', 'path': 'tmp/dir/lookup'}
done
unknown command, ignored: {'script': 'old syntax'}
unknown command, ignored: ['true']
